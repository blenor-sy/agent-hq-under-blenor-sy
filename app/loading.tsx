export default function Loading() {
  return (
    <main className="center-page" aria-live="polite">
      <div className="loader" />
      <span className="sr-only">Loading Agent HQ</span>
    </main>
  );
}
